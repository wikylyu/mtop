# this is a config template

tls:
  listen: ":4433"
  crt: ./keys/server.crt
  key: ./keys/server.key

log:
  level: "debug"

db:
  debug: false
  driverName: psql # mysql
  dsn: "postgres://postgres@127.0.0.1/mtop?sslmode=disable"
